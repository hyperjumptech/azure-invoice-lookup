# Azure tenant ID. Find this in the Overview page of the application you created in App Registrations in Azure Portal. It is shown as Directory (tenant) ID.
AZURE_TENANT_ID=abcd # required

# Azure client ID. Find this in the Overview page of the application you created in App Registrations in Azure Portal. It is shown as Application (client) ID.
AZURE_CLIENT_ID=abcd # required

# Azure client secret. Find this in the Certificates & Secrets page of the application you created in App Registrations in Azure Portal. It is shown as Client secrets.
AZURE_CLIENT_SECRET=abcd # required

# Multiple billing account IDs can be separated by commas. Go to Cost Management + Billing, select one of the billing accounts, click on "Properties" on the sidebar under "Settings", and copy the Billing Account ID.
AZURE_BILLING_ACCOUNT_ID=abcd # required

# Name of the company.
NEXT_PUBLIC_COMPANY_NAME=abcd # required

# Name of the application.
NEXT_PUBLIC_APP_NAME=abcd # required

# Password for the SMTP server. If using Gmail, use the app password.
SMTP_PASSWORD=abcd # required string

# Username for the SMTP server. If using Gmail, use the email address.
SMTP_USERNAME=abcd # required string

# Port for the SMTP server. Usually 465 for SSL or 587 for TLS.
SMTP_PORT=465 # required number

# Host for the SMTP server. Usually smtp.gmail.com for Gmail.
SMTP_HOST=smtp.gmail.com # required string

# The email of sender
SMTP_FROM=email@email.com #default

# The base url that will be used to build the sign in link sent to the recipient's email address
BASE_URL=http://localhost:3000 # required

# JWT secret. Must be at least 32 characters. This is used to sign the JWT tokens. Keep it secret!
JWT_SECRET=abcd # required string

# Allowed email addresses. Comma-separated list of email addresses that are allowed to login.
ALLOWED_EMAIL_ADDRESSES=email@email.com # required

# Whether to send emails. Set to 1 to NOT send emails. Useful for development to avoid sending emails to actual users.
NEXT_PUBLIC_NO_EMAIL_SEND=1

# Key that is used to authorized access to /api/health. Create a random long key for security.
API_HEALTH_KEY=abcd # required

# The environment of node runtime
NODE_ENV=production #default # required

# The comma separated email addresses that will receive the notifications when a user logs in, search for invoice, etc. This is for audit purposes.
NOTIFICATION_RECIPIENT_EMAIL_ADDRESSES=email@email.com #required